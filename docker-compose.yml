services:
  api-gateway:
    image:  ${DOCKER_IMAGE}:dev
    container_name: mmb-apigateway
    ports:
      - "${APIGATEWAY_PORT}:8080"   # 외부 80 -> 내부 8080
    networks:
      - sample-network
    restart: no
    env_file:
      - .env

networks:
  sample-network:
    external: true
    driver: bridge
